---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageIntro from '../components/PageIntro.astro';
import { getChronik } from '../lib/collections';

// Die Chronik liegt als Sammlung unter src/content/chronik/ und ist damit
// redaktionell pflegbar. Zuvor stand sie als 30-stellige Konstante hier.
const timeline = await getChronik();

---

<BaseLayout title="Der Verein" description="Seit 1981 steht die Europa-Show-Tanzgarde für Showtanz, Gardetanz und Vereinsleben in Wassertrüdingen. Unsere Geschichte und was uns ausmacht.">
  <PageIntro title="Die Europa-Show-Tanzgarde" flush tightTop compactTitle />

  <section class="section club-history">
    <div class="wrap club-history__layout">
      <header>
        <h2>Unsere Geschichte</h2>
      </header>
      <div class="club-history__content">
        <ol class="timeline">
          {timeline.map((item) => (
            <li class={`timeline-${item.year}`}>
              <time>{item.year}</time>
              <div><h3 class:list={{
                'timeline__title--nowrap': item.nowrap,
              }}>{item.title}</h3><p>{item.text}</p></div>
            </li>
          ))}
        </ol>
        <div class="european-journeys">
          <h3>Unterwegs in Europa</h3>
          <p>Mittlerweile hat die Europa-Show-Tanzgarde acht europäische Länder besucht:</p>
          <p class="european-journeys__countries">Ungarn, Rumänien, Kroatien, Spanien, Frankreich, England, Belgien und Dänemark</p>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  :global(.page-intro h1) {
    max-width: none;
    font-size: clamp(2rem, 2rem + 3vw, 5.1rem);
    white-space: nowrap;
  }

  :global(.page-intro__inner) {
    grid-template-columns: minmax(0, 1fr);
  }

  .club-history {
    padding-top: var(--space-3);
    padding-bottom: var(--space-4);
    border-top: 0;
  }

  .club-history__layout {
    display: grid;
    gap: var(--space-7) clamp(2rem, 8vw, 8rem);
  }

  @media (min-width: 820px) {
    .club-history__layout {
      grid-template-columns: 15rem minmax(0, 46rem);
      align-items: start;
    }

    .club-history header {
      position: sticky;
      top: 7rem;
    }
  }

  .club-history h2 {
    font-size: var(--step-3);
  }

  .club-history header h2 {
    max-width: none;
    font-size: var(--step-2);
    white-space: nowrap;
  }

  .timeline {
    padding: 0;
    list-style: none;
  }

  .timeline li {
    display: grid;
    grid-template-columns: 7rem minmax(0, 1fr);
    gap: var(--space-4);
    padding-block: var(--space-5);
    border-top: 1px solid var(--hairline-strong);
  }

  .timeline li:first-child {
    border-top: 0;
  }

  .timeline li:last-child {
    border-bottom: 1px solid var(--hairline-strong);
  }

  .timeline time {
    font-family: var(--font-display);
    font-size: var(--step-1);
    font-style: normal;
    font-weight: 800;
    font-variant-numeric: tabular-nums;
    line-height: 1.2;
    color: var(--blue-700);
    white-space: nowrap;
  }

  .timeline h3 {
    font-family: var(--font-display);
    font-size: var(--step-1);
    font-weight: 800;
    line-height: var(--leading-snug);
    color: var(--text);
  }

  .timeline-1982 h3 {
    max-width: none;
    white-space: nowrap;
  }

  @media (min-width: 680px) {
    .timeline__title--nowrap {
      max-width: none;
      white-space: nowrap;
    }
  }

  .timeline p {
    margin-top: var(--space-2);
    font-family: var(--font-body);
    font-size: var(--step-0);
    font-weight: 400;
    line-height: var(--leading-normal);
    color: var(--text-muted);
  }

  .european-journeys {
    padding-top: var(--space-6);
  }

  .european-journeys h3 {
    font-size: var(--step-2);
    color: var(--blue-700);
  }

  .european-journeys p {
    margin-top: var(--space-3);
    color: var(--text-muted);
  }

  .european-journeys .european-journeys__countries {
    font-weight: 750;
    color: var(--text);
  }
</style>
